
<p>index</p>
<p>views/users/index.html</p>







<%= render 'layouts/user_left', book:@book_new %>



<div class='col-md-8 offset-md-1'>
<h2>Users</h2>
<table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <thead>
      <% @users.each do |user| %>
      <tr>
        <td>
          <% if user.image.attached? %>
            <%= image_tag user.image, size: "100x100" %>
          <% else %>
            <%= image_tag 'default-image', size: "100x100" %>
          <% end %>
        </td>
        <td>
          <%= user.name %>
        </td>
        <td>
          <%= link_to 'Show', user_path(user) %>
        </td>
      </tr>
      <% end %>
    </thead>
  </tbody>
</table>